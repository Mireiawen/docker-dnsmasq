# Simple dns config, for a complete example, see:
#  https://oss.segetech.com/intra/srv/dnsmasq.conf

# Log all dns queries
log-queries

# Dont use hosts nameservers
no-resolv

# Use Cloudflare as default nameservers, prefer 1^4
server=1.0.0.1
server=1.1.1.1
strict-order
